# Consolidated Python deps for the large-dataset benchmark harness.
# Requires Python 3.11+ (enforced by scripts/lib/benchmark-python-deps.sh and CI setup-python).
# Covers: workloads (pytest), tpuf_driver (turbopuffer, httpx), cross_check (pytest),
#         validate-benchmark-json.sh (jsonschema).
#
# Install:
#   ./scripts/install-benchmark-python-deps.sh
#   pip install -r benchmarks/requirements.txt

# G4 live driver + Phase 3.3 overlap (live mode).
# Pin: shares the benchmark venv with jsonschema (install-benchmark-python-deps.sh);
# no pip cross-dependency, but keep both pins in sync when bumping either package.
turbopuffer==2.3.0

# tpuf driver offline tests (HTTP client surface)
httpx>=0.27,<1

# Offline harness: workloads + tpuf_driver + cross_check
pytest>=8.0,<10

# scripts/validate-benchmark-json.sh (Draft202012Validator).
# Pin: avoids jsonschema 4.24+ resolver drift in the same venv as turbopuffer==2.3.0;
# verified with `pytest benchmarks/` and `./scripts/validate-benchmark-json.sh`.
jsonschema==4.23.0